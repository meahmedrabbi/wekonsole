import { PlaceholderPage } from '@/components/common';

export default function SecurityPage() {
  return (
    <PlaceholderPage
      title="Security"
      description="Manage SSL certificates, firewall, and security settings"
    />
  );
}
