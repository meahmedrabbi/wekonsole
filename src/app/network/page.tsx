import { PlaceholderPage } from '@/components/common';

export default function NetworkPage() {
  return (
    <PlaceholderPage
      title="Network"
      description="Configure network interfaces, firewall rules, and DNS"
    />
  );
}
