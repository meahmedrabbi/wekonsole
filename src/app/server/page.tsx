import { PlaceholderPage } from '@/components/common';

export default function ServerPage() {
  return (
    <PlaceholderPage
      title="Server"
      description="Monitor server resources, processes, and services"
    />
  );
}
